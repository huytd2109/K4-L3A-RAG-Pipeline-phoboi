# Individual contribution report

Mỗi thành viên copy template này thành:

```text
reports/<student-id>-<short-name>.md
```

Giới hạn khuyến nghị: 1 trang, không chép lại README hoặc mô tả lý thuyết chung. Báo cáo không phải một bài pipeline cá nhân; mục đích là ghi nhận ownership và bằng chứng đóng góp trong sản phẩm nhóm.

---

## Thông tin

- Họ và tên: Đỗ Quốc An
- Mã học viên: 2A202602892
- Nhóm: phoboi
- Repository: https://github.com/huytd2109/K4-L3A-RAG-Pipeline-phoboi
 - Branch : `do-quoc-an-ui`

## Phần việc đã thực hiện

| Module/deliverable | Việc tôi trực tiếp làm | File/commit/PR | Trạng thái |
|---|---|---|---|
| Task 8 — PageIndex fallback | Tích hợp PageIndex làm phương án tìm kiếm dự phòng. Xử lý trường hợp thiếu API key hoặc PageIndex gặp lỗi để ứng dụng không bị dừng. | `src/task8_pageindex_vectorless.py`; commit `39e1060`; PR #2 | Hoàn thành code, chưa chạy với API key thật |
| Task 9 — Retrieval pipeline | Kết hợp tìm kiếm vector và BM25 bằng RRF. Khi kết quả không đủ tốt, hệ thống chuyển sang PageIndex hoặc trả về kết quả an toàn. | `src/task9_retrieval_pipeline.py`; commit `39e1060`; PR #2 | Hoàn thành |
| Giao diện demo | Xây dựng giao diện Streamlit, hiển thị câu trả lời, nguồn trích dẫn, trạng thái hệ thống và các câu hỏi mẫu. | `app.py`; commit `08d5767`; branch `do-quoc-an-ui` | Hoàn thành |

Mô tả tối đa hai quyết định mà bạn trực tiếp tham gia:

1. **Quyết định:** Kết hợp tìm kiếm vector và BM25.  
   **Lý do:** Tìm kiếm vector giúp hiểu ý nghĩa câu hỏi, còn BM25 tìm tốt các từ khóa chính xác như tên địa điểm hoặc số hiệu thông tư.  
   **Trade-off:** Hệ thống xử lý nhiều bước hơn và cần thêm thời gian truy xuất.

2. **Quyết định:** Chỉ gọi PageIndex khi kết quả tìm kiếm chính không đủ tốt.  
   **Lý do:** Giúp PageIndex hoạt động như phương án dự phòng và tránh gọi API không cần thiết. Nếu chưa có API key hoặc PageIndex gặp lỗi, ứng dụng vẫn tiếp tục hoạt động.  
   **Trade-off:** Chưa thể kiểm tra đầy đủ chất lượng PageIndex khi chưa có API key thật.

## Kiểm thử và kết quả

- Test hoặc query tôi đã dùng:
  - Chạy `pytest tests/test_contracts.py -q`.
  - Hỏi về địa điểm du lịch tại Hà Nội.
  - Hỏi nội dung của Thông tư 10/2026/TT-BVHTTDL.
  - Hỏi giá Bitcoin để kiểm tra câu hỏi ngoài phạm vi dữ liệu.

- Kết quả:
  - Đạt `15/15` contract tests.
  - Hệ thống tìm kiếm và trả kết quả đúng định dạng.
  - Câu hỏi thuộc phạm vi dữ liệu có thể trả lời kèm nguồn.
  - Câu hỏi ngoài phạm vi được từ chối an toàn.
  - Khi thiếu PageIndex API key, ứng dụng vẫn hoạt động bình thường.

- Lỗi đã phát hiện và cách xử lý:
  - PageIndex có thể phản hồi chậm nên tôi bổ sung giới hạn thời gian chờ.
  - API key chưa được cấu hình có thể gây lỗi nên tôi bổ sung bước kiểm tra key.
  - Điều chỉnh pipeline để chỉ gọi PageIndex khi kết quả tìm kiếm chính không đủ tốt.

## Điều còn hạn chế

- Hạn chế: Chưa thể kiểm thử PageIndex thực tế vì chưa có API key. Ngưỡng điểm `0.3` mới là giá trị thử nghiệm.
- Nếu có thêm thời gian: Tôi sẽ kiểm thử bằng PageIndex API key thật và điều chỉnh ngưỡng điểm dựa trên nhiều câu hỏi đánh giá hơn.

## Xác nhận đóng góp

Tôi xác nhận nội dung trên phản ánh đúng phần việc của mình và có thể giải thích hoặc chạy lại trong buổi demo.

- Ngày: 20/09/2026
- Tên thành viên: Đỗ Quốc An
